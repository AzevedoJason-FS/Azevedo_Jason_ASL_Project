extends ../../layout/base

block content 
    h1 All Products:
    ul 
        each product in products
            li= product.name + '|$' + product.price        
            a(href='products/'+product.id) Edit 
            a(href='products/'+product.id+'/delete') Delete
    br
    a(href='products/new') Create New Item