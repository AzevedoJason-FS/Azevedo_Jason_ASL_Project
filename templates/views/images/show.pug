extends ../../layout/base

block content 
    h1 About #{product.title}
     ul 
        li 
            label ID: #{product.id}
            br
            label Name: #{product.title}
            br
            label Price: #{product.price}
            br
            a(href=product.id+'/edit') Edit
            a(href=product.id+'/delete') Delete 